import { MSABase } from './parser';
export interface MSA extends MSABase {
    className: string;
    componentName: string;
    tagName: string;
}
export interface SharedData {
    msa?: MSA[];
}
declare function loader(content: string, sourceMap: any, meta?: SharedData): void;
export declare function pitch(skip: any, me: any, sharedData: SharedData): void;
export default loader;
