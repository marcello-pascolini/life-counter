import type { MSA } from './types';
import type { Plugin } from 'vite';
type ResultCacheValue = {
    msa: MSA[];
};
export declare const resultCache: Map<string, ResultCacheValue>;
export default function stylinLoader(): Plugin;
export {};
