import type { MSA } from './types';
declare function parseComments(text: string): MSA[];
declare function extractClassNames(code: string): Record<string, string>;
export { parseComments, extractClassNames };
